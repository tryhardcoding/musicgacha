name: Daily Data Update

on:
  schedule:
    # 毎日 20:00 UTC = JST 05:00
    - cron: '0 20 * * *'
  workflow_dispatch: # 手動実行も可能
  push:
    paths:
      - '.github/workflows/daily-update.yml'

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45 # 無料枠節約（Fetch/Merge/Assign + Enrich 50曲で十分）
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch Top200 data
        run: node scripts/fetch-kworb-top200.js

      - name: Merge Top200 to packs
        run: node scripts/merge-top200-to-packs.js

      - name: Assign rarity
        run: node scripts/assign-rarity.js

      - name: Enrich iTunes data (songs.json, max 50)
        run: node scripts/enrich-itunes-data.js --limit 50

      - name: Enrich iTunes data (Top 200)
        run: node scripts/enrich-itunes-data.js --top200

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --staged --quiet || git commit -m "chore: daily data update $(date -u +%Y-%m-%d)"
          git push

